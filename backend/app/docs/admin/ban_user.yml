用户封禁/解封
---
tags:
  - 后台管理 (Admin)
parameters:
  - name: body
    in: body
    schema:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          description: 用户ID
responses:
  200:
    description: 操作成功 (返回已封禁/已解封)
  404:
    description: 用户不存在