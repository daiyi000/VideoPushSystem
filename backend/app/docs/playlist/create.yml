创建播放列表
---
tags:
  - 播放列表 (Playlist)
parameters:
  - name: body
    in: body
    schema:
      type: object
      required:
        - title
        - user_id
      properties:
        title:
          type: string
        user_id:
          type: integer
responses:
  200:
    description: 创建成功